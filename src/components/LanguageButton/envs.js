import { Icon } from '@chakra-ui/react';
import { EnglishIcon, VietnameseIcon } from '../icons';

export default {
  en: <Icon as={EnglishIcon} boxSize="20px" />,
  vi: <Icon as={VietnameseIcon} boxSize="20px" />,
};
